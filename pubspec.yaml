name: model
version: 0.0.2
description: Manage database easier. Use models to represent rows of database.
  Create database structure using object-oriented dart and leave all SQL strings hidden.
homepage: https://github.com/romanstrazanec/model
repository: https://github.com/romanstrazanec/model

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  path: ^1.7.0
  sqflite: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
